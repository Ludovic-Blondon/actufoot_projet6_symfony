{% extends 'base.html.twig' %}

{% block title %}Acceuil | ActuFoot{% endblock %}

{% block body %}
    <div class="container text-center mt-4">
        <h1>Welcome On ActuFoot</h1>
        <p>Et licet quocumque oculos flexeris feminas adfatim multas spectare cirratas, quibus, si nupsissent, per aetatem ter iam nixus poterat suppetere liberorum, ad usque taedium pedibus pavimenta tergentes iactari volucriter gyris, dum exprimunt innumera simulacra, quae finxere fabulae theatrales.</p>
    </div>
    <div class="container">
        <div class="row">
            {% for  post in posts %}
                {% if loop.first %}
                    <div class="card mb-3 mt-5">
                        <a href="{{ path('post.show', {id: post.id, slug: post.slug}) }}"><img class="card-img-top" src="http://placehold.it/780x180" alt="Card image cap"></a>
                        <div class="card-body">
                            <h3 class="card-title text-center mt-3">
                                <a href="{{ path('post.show', {id: post.id, slug: post.slug}) }}"><strong>{{ post.title | upper }}</strong></a>
                            </h3>
                            <p class="card-text text-justify mt-5">{{ post.content | striptags | slice(0, 400) | raw }} . . .<br><a class="btn btn-primary mt-2" href="{{ path('post.show', {id: post.id, slug: post.slug}) }}">lire la suite</a></p>
                            <p class="card-text"><small class="text-muted">Posté le {{ post.postAt | date("m/d/Y à h:m:s") }} par {{ post.author }}</small></p>
                        </div>
                    </div>
                {% else %}
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card h-100">
                        <a href="{{ path('post.show', {id: post.id, slug: post.slug}) }}"><img class="card-img-top" src="http://placehold.it/700x400" alt=""></a>
                        <div class="card-body">
                            <h4 class="card-title text-center">
                                <a href="{{ path('post.show', {id: post.id, slug: post.slug}) }}"><strong>{{ post.title | upper }}</strong></a>
                            </h4>
                            <p class="card-text text-justify">{{ post.content | striptags | slice(0, 200) | raw }} . . .<br><a class="btn btn-primary mt-2" href="{{ path('post.show', {id: post.id, slug: post.slug}) }}">lire la suite</a></p>
                        </div>
                        <div class="card-footer">
                            <p class="card-text"><small class="text-muted">Posté le {{ post.postAt | date("d/m/Y à h:m:s") }} par {{ post.author }}</small></p>
                        </div>
                    </div>
                </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
{% endblock %}
